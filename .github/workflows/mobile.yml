name: Mobile App CI

on:
  push:
    paths:
      - 'nexa-mobile/**'
      - '.github/workflows/mobile.yml'
  pull_request:
    paths:
      - 'nexa-mobile/**'

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: nexa-mobile/package.json
      - name: Install dependencies
        working-directory: nexa-mobile
        run: npm install
      - name: Typecheck
        working-directory: nexa-mobile
        run: npm run typecheck
