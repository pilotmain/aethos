# Adaptive provider routing (Phase 4 Step 10)

`runtime_provider_routing.py` exposes advisory fallback chains, decision explanations, and effectiveness scores.

**API:** `GET /api/v1/runtime/routing`  
**CLI:** `aethos runtime routing`

Orchestrator-controlled; paid fallback requires operator approval when configured.
