# Branding convergence (Phase 4 Step 10)

User-facing surfaces use **AethOS** terminology. Legacy env aliases (`NEXA_*`) remain for compatibility.

`runtime_identity_lock.py` locks canonical labels.

README includes a single inspiration line: *Inspired by OpenClaw.* Parity docs remain internal.
