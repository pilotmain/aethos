# Execution visibility (Phase 3 Step 14)

Unified operational execution chains on runtime truth:

- `execution_visibility` — summary + trace health
- `execution_chains` — operator → orchestrator → worker → provider → deliverable
- `execution_governance` — governance attachment to execution
- `execution_trace_health` — bounded trace counts

API: `GET /mission-control/execution/visibility`  
CLI: `aethos execution visibility`
