# Installer branding convergence

User-facing setup is **AethOS-native**.

| Legacy | Allowed |
|--------|---------|
| Nexa | `NEXA_*` env aliases only |
| OpenClaw | README + parity tests only |

Audit: `setup_branding_convergence` on runtime truth; `aethos setup certify` branding scans.
