# Installer error experience

Prefer:

> AethOS could not validate this provider yet. You can retry now or continue setup and configure it later.

Avoid:

> ERROR: connection failed

Use `aethos setup doctor` and `aethos doctor` for health checks after setup.
