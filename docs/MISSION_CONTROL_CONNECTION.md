# Mission Control connection

Setup seeds:

- `API_BASE_URL`, `NEXA_WEB_API_TOKEN`, `TEST_X_USER_ID` / `X_USER_ID`
- `web/.env.local` with `NEXT_PUBLIC_AETHOS_API_BASE`
- Setup creds JSON for the web bootstrap flow

**CLI:** `aethos connect`, `aethos connection show`, `aethos connection repair`
