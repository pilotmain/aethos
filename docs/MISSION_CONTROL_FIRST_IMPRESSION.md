# Mission Control first impression

After setup, Mission Control should already have:

- API URL and bearer token seeded
- User id configured
- Onboarding state present
- Runtime validated

**API:** `GET /api/v1/setup/first-impression`  
**CLI:** `aethos setup first-impression`

Greeting includes operator name when onboarding profile exists.
