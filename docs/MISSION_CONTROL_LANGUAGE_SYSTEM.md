# Mission Control language system (Phase 4 Step 9)

Canonical enterprise-friendly terminology for MC surfaces (e.g. `degraded` → “Needs attention”, `runtime_pressure` → “Operational load”).

Module: `mission_control_language_system.py`  
Applied via `apply_user_facing_language()` on executive and summary payloads.

Truth key: `mission_control_language_system`.
