# Office operational streaming

Staggered panel refresh intervals prevent jittery full-page reloads. Configured via `office_operational_stream` on Office API responses.

See `office_operational_stream.py`.
