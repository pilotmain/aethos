# Onboarding profile memory

Stored at `~/.aethos/onboarding_profile.json` as `{"v":1,"profile":{...}}`.

Used by Mission Control first-run welcome and operational personalization. Not sent to providers without explicit routing.

**API:** `GET /api/v1/setup/operator-profile`
