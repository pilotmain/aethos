# Operational command experience

```bash
aethos restart              # API + web (default)
aethos restart api|web|bot|runtime|connection
aethos doctor
aethos setup status
aethos setup resume
aethos setup repair
```

Commands explain progress and avoid noisy logs where possible.
