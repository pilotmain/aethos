# Operator first-run experience

1. Complete `aethos setup` (conversational, resumable)
2. Open Mission Control — welcome + recommended next steps
3. Visit **The Office** — operational command center
4. Optional: `aethos setup onboarding` for relationship questions

No manual API URL / token / user id unless customizing.
