# Plugin vs marketplace — final (Phase 4 Step 12)

| Type | Meaning |
|------|---------|
| Runtime plugin | Extends runtime/provider capabilities (`/mission-control/plugins`) |
| Automation pack | Operator-triggered operational workflow (`/mission-control/operational-insights`) |
| Marketplace skill | Installable AI execution package (`/mission-control/marketplace`) |

Mission Control marketplace page shows all three distinctions.
