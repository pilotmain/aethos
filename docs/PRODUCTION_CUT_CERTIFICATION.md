# Production cut certification

```bash
aethos setup certify
aethos runtime certify
```

API: `GET /api/v1/runtime/certify`

Module: `production_cut_certification.py`

Outputs `enterprise_grade` and `production_cut_ready` with category blockers.
