# Runtime escalations

Truth keys:

- `runtime_escalations` — active escalation list
- `escalation_visibility` — operator-readable summary
- `escalation_history` — bounded history

Types: deployment, provider, privacy, retry, worker, governance, runtime degradation.

API: `GET /mission-control/runtime/escalations`  
CLI: `aethos runtime escalations`
