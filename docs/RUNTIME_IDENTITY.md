# Runtime identity (Phase 3 Step 15)

Canonical operational vocabulary on runtime truth via `runtime_identity`:

- **AethOS Orchestrator** — central orchestration identity
- Unified labels for workers, governance, providers, automation, deliverables
- `health_label` and `trust_label` for consistent Mission Control copy

Truth key: `runtime_identity` with `CANONICAL_LABELS` in `runtime_identity.py`.
