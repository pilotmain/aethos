# Runtime ownership authority (Phase 4 Step 25)

`app/services/runtime/runtime_ownership_authority.py` is the single coordinator for API, Telegram, hydration, and SQLite ownership truth keys.

Operator commands: `aethos runtime ownership`, `aethos runtime recover`, `aethos repair`.
