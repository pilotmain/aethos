# Runtime payload profiles

Profiles: `minimal`, `office`, `operational`, `enterprise`, `full`.

`apply_payload_profile()` in `runtime_payload_profiles.py` filters truth for page-specific payloads.

`GET /api/v1/runtime/payloads` returns byte sizes per profile.
