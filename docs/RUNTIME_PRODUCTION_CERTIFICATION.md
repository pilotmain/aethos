# Runtime production certification (Step 23)

`GET /api/v1/runtime/production-certification` — final production-cut verdict.

CLI: `aethos runtime production-cert`  
Operator trust: `GET /api/v1/runtime/operator-trust` · `aethos runtime trust`
