# Runtime recovery center

`GET /api/v1/mission-control/runtime-recovery` exposes failed slices, hydration metrics, stale caches, and advisory recovery recommendations.

Mission Control: `/mission-control/runtime-recovery`
