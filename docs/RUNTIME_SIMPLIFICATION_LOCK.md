# Runtime simplification lock (Phase 4 Step 21)

`runtime_simplification_lock.py` records canonical runtime systems and duplicate surfaces to avoid (e.g. overlapping readiness banners).

API: `GET /api/v1/runtime/simplification-lock`
