# Runtime supervision verification

```bash
scripts/verify_runtime_supervision.sh
aethos runtime ownership
aethos runtime services
aethos doctor
```

Mission Control: `/mission-control/runtime-supervision` — API `GET /api/v1/runtime/supervision`.
