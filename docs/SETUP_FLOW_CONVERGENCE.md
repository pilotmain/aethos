# Setup flow convergence

**Experience API:** `GET /api/v1/setup/experience` — conversational flow model (8 guided stages).

Entrypoints unified under `aethos setup` with subcommands: `status`, `continuity`, `onboarding`, `first-impression`, `certify`, `doctor`, `validate`, `resume`, `repair`.

Phase: `phase4_step15`.
