# UI branding purge — final (Phase 4 Step 12)

Scan: `app/services/setup/ui_branding_purge_final.py`

User-facing copy uses **AethOS**. Remaining `Nexa` allowed only in type aliases (`NexaWebConfig`), API path comments, and legacy internal identifiers.

Verify: `pytest tests/test_ui_branding_purge_final.py`
