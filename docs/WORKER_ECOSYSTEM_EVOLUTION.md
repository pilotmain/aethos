# Worker ecosystem evolution

Worker ecosystem health, coordination quality, specialization maps, and growth metrics.

Truth keys: `worker_ecosystem_health`, `worker_coordination_quality`, `worker_specialization_map`, `worker_operational_growth`.

**API:** `GET /api/v1/mission-control/workers/ecosystem`  
**CLI:** `aethos workers ecosystem`
