/** Reserved for future roster APIs bridged from orchestration / Telegram scope. */

export async function placeholderTeamNotice(): Promise<string> {
  return 'Team roster sync can map orchestration agents to mobile in a later iteration.';
}
